meta {
  name: List virtual keys
  type: http
  seq: 1
}

get {
  url: {{URL}}/v1/admin/virtual-keys
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Secrets are write-only. Each row returns `display_prefix` only — never the key.
}
