meta {
  name: Update routing
  type: http
  seq: 4
}

put {
  url: {{URL}}/v1/admin/virtual-keys/:id/routing
  body: json
  auth: inherit
}

params:path {
  id: 01a0c31c-d26e-7a83-a7c6-ba212d9974a5
}

body:json {
  {
    "is_active": true
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Every field is optional — send only what changes. Omitted fields keep their
  current value.
  
  Changes the routing only. It does not touch the secret, the client or the budget.
}
